# Docs Archive

This folder contains legacy/reference documents (often exported from `.docx`) that are **not** canonical operating guidance for the current build.

Use these only as historical context or raw source material — do not treat them as live page specs.

Canonical guidance lives in:
- `AGENT_SYSTEM.md`
- `docs/agent/*`
- `agent-system/context/*`

